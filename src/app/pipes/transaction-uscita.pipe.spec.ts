import { TransactionUscitaPipe } from './transaction-uscita.pipe';

describe('TransactionUscitaPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionUscitaPipe();
    expect(pipe).toBeTruthy();
  });
});
