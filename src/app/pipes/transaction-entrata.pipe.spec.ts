import { TransactionEntrataPipe } from './transaction-entrata.pipe';

describe('TransactionEntrataPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionEntrataPipe();
    expect(pipe).toBeTruthy();
  });
});
